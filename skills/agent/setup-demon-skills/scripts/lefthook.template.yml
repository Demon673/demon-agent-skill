# Git hooks (lefthook). Keep these local checkpoints fast; CI owns the full
# repository-wide gate matrix.
# The first two jobs need the pack's pairing and archive gates copied into the
# host's scripts/ first (setup ships only the budget gate); the whitespace job
# runs everywhere.
pre-commit:
  jobs:
    - name: translation pairing (staged records)
      glob: '*.i18n.yaml'
      exclude:
        - '.agents/notes/archived/**'
      run: node scripts/verify-translation-pairing.mjs --check {staged_files}

    - name: archived agent notes
      glob: '.agents/notes/archived/**'
      run: node scripts/verify-archived-agent-notes.mjs

    - name: whitespace (staged)
      run: git diff --cached --check
