interface:
  display_name: "Ask Demon"
  short_description: "Router over the docs-maintenance flow, the doc governance run, and game skills"
  default_prompt: "Use $ask-demon to find the right skill or flow."
policy:
  allow_implicit_invocation: false
